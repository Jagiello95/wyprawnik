import { AcceptEditRowDirective } from './accept-row.directive';

describe('AcceptEditRowDirective', () => {
  it('should create an instance', () => {
    const directive = new AcceptEditRowDirective();
    expect(directive).toBeTruthy();
  });
});
