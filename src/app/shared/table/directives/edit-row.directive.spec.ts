import { EditRowDirective } from './edit-row.directive';

describe('EditRowDirective', () => {
  it('should create an instance', () => {
    const directive = new EditRowDirective();
    expect(directive).toBeTruthy();
  });
});
