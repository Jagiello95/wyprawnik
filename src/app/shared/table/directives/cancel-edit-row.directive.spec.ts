import { CancelEditRowDirective } from './cancel-edit-row.directive';

describe('CancelEditRowDirective', () => {
  it('should create an instance', () => {
    const directive = new CancelEditRowDirective();
    expect(directive).toBeTruthy();
  });
});
